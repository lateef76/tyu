// Placeholder for any future JavaScript functionality
document.querySelectorAll('.plan button').forEach(button => {
    button.addEventListener('click', () => {
        alert('You clicked on Invest Now!');
    });
});